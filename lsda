#!/bin/bash
# list *all* directories, since `ls` doesn't provide a flag/option for this

if [ $# -eq 0 ]; then
  find -maxdepth 1 -type d | sed 's/\.\///g' | sort
else
  find $1 -maxdepth 1 -type d | sort
fi
